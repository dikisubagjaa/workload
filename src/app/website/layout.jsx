export const metadata = {
    title: 'VP Digital | Website',
    description: 'VP Digital | Website',
}

export default function layout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
