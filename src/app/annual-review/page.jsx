import MainContent from "./MainContent";

export const metadata = {
    title: 'Annual Review',
    description: 'Annual Review Page',
}

export default function page() {
    return (
        <MainContent />
    )
}
